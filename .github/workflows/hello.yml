---
name: Hello
on:
  push:
    branches:
      - main
jobs: null
greet:
  runs-on: ubuntu-latest
  steps:
    - name: say hello to users
      run: echo "Hello $user"
    - name: say hello to username
      run: echo "Hello users"
      
      
     
